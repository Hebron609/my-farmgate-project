import { createApp } from "vue";
import TermsAndConditions from "./pages/TermsAndCondition.vue";
import "./style.css";
import { setupApp } from "./shared-setup";

setupApp(TermsAndConditions, "#app");
